# 旧フレームワーク参照（親 PJ との対応表）

> 担当: E 進行チーム（archivist-pm）
> 起票: 2026-08-29
> 目的: `minmax-fx-day-trading-lab`（親 PJ）の旧評価系コードと本 PJ の新評価系の対応表

## 1. ファイル対応表

| 親 PJ ファイル | 親 PJ パス | 本 PJ ファイル | 本 PJ パス | 差分 |
|---|---|---|---|---|
| `criteria.py` | `src/minmax_fx_dt/decision/criteria.py` | `criteria.py` | `src/minmax_fx_eval/decision/criteria.py` | v0.2 閾値適用・DSR 必須ゲート追加 |
| `permutation.py` | `src/minmax_fx_dt/backtest/permutation.py` | `permutation.py` | `src/minmax_fx_eval/statistics/permutation.py` | block デフォルト化・clustered `@deprecated` |
| （旧：親 PJ に存在せず） | — | `dsr.py` | `src/minmax_fx_eval/statistics/dsr.py` | **新規**: Bailey & Lopez de Prado 2014 実装 |
| （旧：MIN_N_TRADES_STATISTICAL = 300 ハードコード） | `criteria.py:75` | `power.py` | `src/minmax_fx_eval/statistics/power.py` | **新規**: 動的検出力計算 |
| `metrics.py` | `src/minmax_fx_dt/backtest/metrics.py` | `metrics.py` | `src/minmax_fx_eval/backtest/metrics.py` | 必要部分のみフォーク（変更なし） |

## 2. 親 PJ からの流用（変更なし・参照のみ）

| 親 PJ ファイル | 利用方法 |
|---|---|
| `simulator.py` | 親 PJ から import（バックテストエンジンは親 PJ 側で保守） |
| `gmo_fx_client.py` | 親 PJ から import（API クライアントは親 PJ 側で保守） |
| `indicators.py` | 親 PJ から import（指標計算は親 PJ 側で保守） |

## 3. KPI 閾値対応表（v0.1 → v0.2）

| KPI | v0.1 (親 PJ) | v0.2 (本 PJ) | 変更理由 |
|---|---|---|---|
| K1m_sharpe | ≥ 0.4 | ≥ 0.4 | 変更なし |
| K1m_pf | ≥ 1.2 | ≥ 1.2 | 変更なし |
| K1m_expectancy | > 0 円 | > 0 円 | 変更なし |
| K2m_dd_monthly | ≤ 10% | ≤ 10% | 変更なし（ただし T-10 で「ピーク比」固定） |
| K2m_dd_yearly | ≤ 20% | ≤ 20% | 変更なし |
| K3m_max_consec | ≤ 5（絶対件数） | i.i.d. 上位 5% パーセンタイル | T-08 適用（既に親 PJ 実装済） |
| **K4m_payoff** | **≥ 1.5** | **≥ 1.2** | **v0.2 緩和（本 spec 2.1）** |
| K5m_spread_cost | ≥ 3.0 | ≥ 3.0 | 変更なし |
| min_n_trades | ≥ 300 | **n ≥ 50 hard floor**（DSR で動的補正） | v0.2 緩和（本 spec 2.2） |
| perm_p | < 0.05 | < 0.05 | 変更なし（ただし block デフォルト） |
| **DSR (NEW)** | — | **≥ 0.95** | **v0.2 新設必須（本 spec 2.4）** |

## 4. 親 PJ 過去判定への影響（v0.2 遡及計算方針）

v0.2 適用時の**判定結果は原則変えない**方針。DSR 値のみ追記する:

- 過去 11 戦略（REJECT/SYS-FX011 保留/SYS-FX012 フォワード中）の DSR 値を `research/フレームワーク再設計/03-過去判定遡及/` に保存
- 親 PJ の `portfolio-ledger.md` に DSR 列を追加（破壊的変更を避けるため、列追加のみ）
- 司令塔が v0.2 フレームワークで判定を見直したい場合は明示的に指示

## 5. マージ手順（親 PJ への取り込み）

1. 本 PJ の `src/minmax_fx_eval/` を親 PJ の `src/minmax_fx_dt/` へコピー
2. 親 PJ の `decision/criteria.py` を本 PJ 実装で置換
3. 親 PJ の `backtest/permutation.py` を本 PJ 実装で置換
4. 親 PJ の `decision/__init__.py` と `backtest/__init__.py` の import を新構造に合わせて更新
5. 親 PJ の既存テスト（25 件）+ 本 PJ のテスト（6 件）の合計 31 件を CI で green 化
6. 親 PJ の `portfolio-ledger.md` に v0.2 適用を明記
